
#include <stdio.h>
#include <math.h>

int main() {
    int p, result;
    printf("Введіть розрядність числа:\n");
    scanf("%d", &p);
    result = pow(p,2) - ((p - 2) * 3); 
    printf("%d", result);
    return 0;
}
